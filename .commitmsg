feat: 포트폴리오 API + 토스트 + 스켈레톤 + AI추천 연결

포트폴리오 API:
- GET/POST /api/instructors/me/portfolio
- JSON 필드로 저장 (이미지/PDF → Vercel Blob, 영상 → URL)
- 최대 25개 제한 + Rate Limiting

글로벌 토스트:
- ToastProvider (Context API)
- 성공/에러/정보 3가지 타입
- 스프링 애니메이션 + 3초 자동 닫힘
- Providers.tsx에 ToastProvider 추가

스켈레톤:
- Skeleton 베이스 + shimmer 애니메이션
- InstructorCardSkeleton, StatCardSkeleton, ListItemSkeleton

연결:
- 교사 홈에 AI 추천 버튼 (보라색)
- 강사 메뉴에 포트폴리오 항목 추가

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>